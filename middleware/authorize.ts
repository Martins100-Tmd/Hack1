// // middlewares/authorize.ts
// import type { IUser } from "../models/user";

// export const authorize = (user: IUser, roles: string[]) => {
//     if (!roles.includes(user.)) {
//         throw new Error("Forbidden: Insufficient permissions");
//     }
// };
